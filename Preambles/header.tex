% =============================================================================
% header.tex — BSAD 8310: Business Forecasting
% University of Nebraska at Omaha
% Beamer theme: UNO-branded, clean, professional
% =============================================================================

% ----------------------------- BEAMER THEME ----------------------------------
\usetheme{default}
\useinnertheme{rectangles}

% ----------------------------- UNO COLOR PALETTE -----------------------------
% Brand colors. UNO's official colors are crimson and black
% (crimson PMS 186 / #D71920). Everything structural draws from these two.
\definecolor{unocrimson}{HTML}{D71920}      % primary brand
\definecolor{unoblack}{HTML}{1A1A1A}        % secondary brand — titles, body
\definecolor{unogray}{HTML}{525252}         % muted text, subtitles
\definecolor{unolightcrimson}{HTML}{FDECEA} % keybox tint
\definecolor{unolightgray}{HTML}{F4F4F5}    % definitionbox tint
\definecolor{unowhite}{HTML}{FFFFFF}

% Semantic accents. Warnings deliberately do NOT use crimson — crimson is the
% brand color and carries no alarm meaning here, so amber does that job.
\definecolor{unoamber}{HTML}{B45309}
\definecolor{unolightamber}{HTML}{FEF3E2}
\definecolor{unogreen}{HTML}{15803D}
\definecolor{unolightgreen}{HTML}{F0FAF4}

% Data-visualization colors. NOT brand chrome — these exist so TikZ plots and
% diagrams have distinguishable series. Do not use them for slide furniture.
\definecolor{unoblue}{HTML}{005CA9}
\definecolor{unolightblue}{HTML}{E8F0FA}

% Backward-compatible aliases used by existing lecture TikZ code.
\colorlet{unored}{unocrimson}
\colorlet{unolightred}{unolightcrimson}

% Apply UNO colors to Beamer structure
\setbeamercolor{structure}{fg=unocrimson}
\setbeamercolor{palette primary}{bg=unoblack, fg=white}
\setbeamercolor{palette secondary}{bg=unoblack!85, fg=white}
\setbeamercolor{palette tertiary}{bg=unoblack!70, fg=white}
% NOTE: the frametitle template below draws no background bar, so the
% foreground must be a DARK color. A white fg here renders invisible.
\setbeamercolor{frametitle}{fg=unoblack}
\setbeamercolor{title}{fg=unocrimson}
\setbeamercolor{subtitle}{fg=unogray}
\setbeamercolor{author in head/foot}{bg=unoblack, fg=white}
\setbeamercolor{title in head/foot}{bg=unoblack!85, fg=white}
\setbeamercolor{date in head/foot}{bg=unoblack!70, fg=white}
\setbeamercolor{page number in head/foot}{bg=unoblack!70, fg=white}
\setbeamercolor{block title}{bg=unocrimson, fg=white}
\setbeamercolor{block body}{bg=unolightcrimson}
\setbeamercolor{block title alerted}{bg=unoamber, fg=white}
\setbeamercolor{block body alerted}{bg=unolightamber}
\setbeamercolor{block title example}{bg=unogreen, fg=white}
\setbeamercolor{block body example}{bg=unolightgreen}
\setbeamercolor{itemize item}{fg=unocrimson}
\setbeamercolor{itemize subitem}{fg=unogray}
\setbeamercolor{enumerate item}{fg=unocrimson}
\setbeamercolor{enumerate subitem}{fg=unogray}
\setbeamercolor{alerted text}{fg=unoamber}
% Table of contents: entry text in black, numbered squares keep the crimson accent.
\setbeamercolor{section in toc}{fg=unoblack}
\setbeamercolor{subsection in toc}{fg=unoblack}
\setbeamercolor{section number projected}{bg=unocrimson, fg=white}

% ----------------------------- FONTS -----------------------------------------
\usefonttheme{professionalfonts}
\usefonttheme[onlymath]{serif}       % serif math; sans-serif text
\setbeamerfont{frametitle}{size=\large, series=\bfseries}
\setbeamerfont{title}{size=\LARGE, series=\bfseries}
\setbeamerfont{subtitle}{size=\large}
\setbeamerfont{block title}{size=\normalsize, series=\bfseries}
\setbeamerfont{footline}{size=\tiny}

% ----------------------------- LAYOUT ----------------------------------------
\setbeamersize{text margin left=0.5cm, text margin right=0.5cm}
\setbeamertemplate{navigation symbols}{}   % remove navigation buttons
\setbeamertemplate{itemize items}[circle]
\setbeamertemplate{enumerate items}[default]

% Custom footline: [Course] [Title] [Page/Total]
\setbeamertemplate{footline}{%
  \leavevmode%
  \hbox{%
    \begin{beamercolorbox}[wd=.33\paperwidth, ht=2.5ex, dp=1ex, left, leftskip=4pt]
      {author in head/foot}%
      \usebeamerfont{author in head/foot}\insertshortauthor
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.34\paperwidth, ht=2.5ex, dp=1ex, center]
      {title in head/foot}%
      % \insertshorttitle is wrapped in a hyperlink, so hyperref's linkcolor
      % wins over any \color. Override linkcolor locally or it renders crimson
      % on the dark footer, which is unreadable.
      \usebeamerfont{title in head/foot}%
      \begingroup\hypersetup{linkcolor=white}\color{white}\insertshorttitle\endgroup
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.33\paperwidth, ht=2.5ex, dp=1ex, right, rightskip=4pt]
      {date in head/foot}%
      \usebeamerfont{date in head/foot}%
      \insertframenumber{} / \inserttotalframenumber
    \end{beamercolorbox}%
  }%
  \vskip0pt%
}

% Frametitle with thin accent line
\setbeamertemplate{frametitle}{%
  \vskip0.1cm
  {\color{unoblack}\insertframetitle}%
  \vskip0.05cm
  \color{unocrimson}\rule{\textwidth}{0.8pt}
}

% Title page
\setbeamertemplate{title page}{%
  \vfill
  \begin{center}
    {\color{unocrimson}\rule{\textwidth}{2.5pt}}\\[0.3cm]
    {\usebeamerfont{title}\usebeamercolor[fg]{title}\inserttitle}\\[0.2cm]
    {\usebeamerfont{subtitle}\usebeamercolor[fg]{subtitle}\insertsubtitle}\\[0.3cm]
    {\color{unoblack}\rule{\textwidth}{0.5pt}}\\[0.4cm]
    {\small\insertauthor}\\[0.1cm]
    {\small\insertinstitute}\\[0.1cm]
    {\small\insertdate}
  \end{center}
  \vfill
}

% ----------------------------- PACKAGES --------------------------------------

% Math
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{bm}                    % bold math symbols

% Graphics & color
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes, fit, backgrounds, calc}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% Tables
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}
\usepackage{tabularx}

% Typography
\usepackage{microtype}
\usepackage{url}
\usepackage{hyperref}
% linkcolor governs INTERNAL links — table-of-contents entries, cross-references,
% and the footline short title. hyperref wins over \setbeamercolor and \color, so
% this is the only place those can be controlled. Keep it black; crimson is
% reserved for real URLs.
\hypersetup{colorlinks=true, linkcolor=unoblack, urlcolor=unocrimson, citecolor=unogray}

% Code listings (no shell-escape required)
\usepackage{listings}
\lstset{
  language=Python,
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{unocrimson}\bfseries,
  stringstyle=\color{unogreen},
  commentstyle=\color{unogray}\itshape,
  numberstyle=\tiny\color{unogray},
  breaklines=true,
  showstringspaces=false,
  frame=single,
  rulecolor=\color{unogray!40},
  backgroundcolor=\color{unogray!5},
  xleftmargin=0.5em,
  xrightmargin=0.5em,
}

% Bibliography
\usepackage[backend=bibtex, style=authoryear, maxcitenames=2]{biblatex}
\addbibresource{../Bibliography_base.bib}

% Colored text helpers
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable, listingsutf8}

% ----------------------------- CUSTOM ENVIRONMENTS ---------------------------

% keybox: crimson left-rule on a pale tint — key results, rules, takeaways
\newtcolorbox{keybox}{
  enhanced,
  colback=unolightcrimson,
  colframe=unocrimson,
  coltext=unoblack,
  fonttitle=\bfseries,
  boxrule=0pt,
  leftrule=3pt,
  arc=0pt,
  left=4pt, right=4pt, top=3pt, bottom=3pt,
}

% definitionbox: neutral dark left-rule with title — for formal definitions
\newtcolorbox{definitionbox}[1]{
  enhanced,
  title={#1},
  colback=unolightgray,
  colframe=unoblack,
  colbacktitle=unolightgray,   % title sits on the body tint, not a dark bar
  coltitle=unoblack,           % (must differ from colbacktitle or it vanishes)
  fonttitle=\bfseries,
  boxrule=0pt,
  leftrule=3pt,
  arc=0pt,
  left=4pt, right=4pt, top=3pt, bottom=3pt,
}

% warningbox: amber left-rule — pitfalls, assumption violations, common errors
\newtcolorbox{warningbox}{
  enhanced,
  colback=unolightamber,
  colframe=unoamber,
  coltitle=unoamber,
  fonttitle=\bfseries,
  boxrule=0pt,
  leftrule=3pt,
  arc=0pt,
  left=4pt, right=4pt, top=3pt, bottom=3pt,
}

% examplebox: green-accent with title — for worked examples, business applications
\newtcolorbox{examplebox}[1]{
  enhanced,
  title={#1},
  colback=unolightgreen,
  colframe=unogreen,
  colbacktitle=unolightgreen,  % title sits on the body tint, not a solid bar
  coltitle=unogreen,           % (must differ from colbacktitle or it vanishes)
  fonttitle=\bfseries,
  boxrule=0pt,
  leftrule=3pt,
  arc=0pt,
  left=4pt, right=4pt, top=3pt, bottom=3pt,
}

% ----------------------------- MATH SHORTCUTS --------------------------------
\newcommand{\E}{\mathbb{E}}
\newcommand{\Var}{\operatorname{Var}}
\newcommand{\Cov}{\operatorname{Cov}}
\newcommand{\Corr}{\operatorname{Corr}}
\newcommand{\MSE}{\operatorname{MSE}}
\newcommand{\RMSE}{\operatorname{RMSE}}
\newcommand{\MAE}{\operatorname{MAE}}
\newcommand{\MASE}{\operatorname{MASE}}
\newcommand{\yhat}{\hat{y}}
\newcommand{\bhat}{\hat{\beta}}
\newcommand{\eps}{\varepsilon}
\newcommand{\given}{\,|\,}

% ----------------------------- SLIDE HELPERS ---------------------------------
% Section title slide (call at start of each section)
\newcommand{\sectionslide}[2]{%
  \begin{frame}
    \vfill
    \begin{center}
      {\color{unocrimson}\rule{0.6\textwidth}{2.5pt}}\\[0.4cm]
      {\Large\bfseries\color{unoblack} #1}\\[0.2cm]
      {\normalsize\color{unogray} #2}\\[0.4cm]
      {\color{unocrimson}\rule{0.6\textwidth}{1pt}}
    \end{center}
    \vfill
  \end{frame}
}

% Muted text
\newcommand{\muted}[1]{{\color{unogray}#1}}

% Key term
\newcommand{\key}[1]{{\color{unocrimson}\textbf{#1}}}

% Positive / negative annotations
\newcommand{\pos}[1]{{\color{unogreen}#1}}
\newcommand{\negc}[1]{{\color{unoamber}#1}}

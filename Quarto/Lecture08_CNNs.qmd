---
title: "CNN Architectures"
subtitle: "ECON 8310: Business Forecasting --- Lecture 8"
author: "Department of Economics"
institute: "University of Nebraska at Omaha"
format:
  revealjs:
    theme: [default, uno-clean.scss]
    slide-number: true
    show-slide-number: all
    transition: fade
    transition-speed: fast
    incremental: false
    toc: false
    center: false
    width: 1280
    height: 720
    margin: 0.05
    logo: ""
    footer: "ECON 8310 | University of Nebraska at Omaha"
    embed-resources: true
bibliography: ../Bibliography_base.bib
csl: https://www.zotero.org/styles/chicago-author-date
---

## Lecture Outline

1. Convolution and Pooling
2. The Architecture Lineage
3. 1D CNNs for Time Series
4. Key Takeaways

---

# Convolution and Pooling

Two ideas — look locally, and reuse what you learn everywhere.

---

## Why Convolution?

Start with why a fully connected layer fails on structured input. A $224 \times 224$ image has 50,176 pixels, so a single hidden layer of 1,000 neurons needs **50 million weights** — for one layer. Lecture 7's network had 22,657 parameters in total.

Worse than the cost is the waste. That layer ignores **spatial structure**: it has no notion that neighbouring pixels are related and distant ones usually are not. And it does not **generalize position** — a pattern learned in the top-left corner is stored separately from the same pattern in the bottom-right.

::: {.keybox}
A convolutional layer fixes both with two ideas. **Local connectivity:** each neuron sees only a small **receptive field**. **Weight sharing:** the same filter is applied at every position. Far fewer parameters, and a pattern learned anywhere is recognized everywhere.
:::

The analogy that makes it stick: a reader does not memorize the letter "A" separately for every position on the page. They learn it once and apply it everywhere. A filter does exactly that.

---

## How a Convolutional Layer Works

For a sequence, a 1D convolution slides a filter of length $K$ along the series and takes a dot product at each position:

$$(f * g)[t] = \sum_{k=0}^{K-1} f[k] \cdot g[t - k]$$

| | |
|---|---|
| **Kernel size** | Filter length $K$. Larger means a wider receptive field — more context per output, and more parameters. |
| **Stride** | How far the filter moves between positions. Stride 2 halves the output length. |
| **Padding** | Zeros at the edges. `padding='same'` keeps output length equal to input length. |
| **Filters** | How many different patterns to learn. This becomes the number of output channels. |

Each filter learns one **local pattern** — an edge, a spike, a seasonal dip. Its output is a **feature map** whose high values mark where that pattern was found.

---

## Pooling: Downsampling Feature Maps

**Pooling** reduces a feature map by summarizing each small region with a single number. Max pooling takes the largest value in the window; average pooling takes the mean. A $2\times2$ max pool with stride 2 quarters the number of values.

Two things are bought. **Fewer parameters downstream**, because later layers see a smaller input. And **translation invariance**: shift the input slightly and the pooled output barely changes, so the network recognizes a cat whether it sits left or right of centre.

::: {.warningbox}
**Translation invariance is a feature for images and a problem for forecasting.** If a demand spike moved three weeks later, that is not a nuisance to be smoothed away — it is the thing you are trying to predict. Pooling deliberately discards *where* in the window a pattern occurred, and for a time series the position is often the signal.
:::

Hold that objection. We measure exactly what it costs later in this lecture.

---

# The Architecture Lineage

Twenty years of image models, and the three ideas that outlived them.

---

## LeNet to VGG: Depth and Small Filters

**LeNet-5** [@LeCun1998] established the template in 1998: alternating convolution and pooling layers, then fully connected layers at the end. It had roughly 60,000 parameters and read handwritten digits on cheques.

**VGG** [@Simonyan2015] scaled that idea up in 2014 and made one sharp simplification: use *only* $3\times3$ filters, but stack many of them. Two stacked $3\times3$ layers see the same receptive field as one $5\times5$, with fewer parameters and an extra nonlinearity in between.

::: {.keybox}
**The lesson that carried:** depth built from small filters beats width built from large ones. It is cheaper in parameters and more expressive, and it is why almost every modern architecture is deep and narrow rather than shallow and wide.
:::

VGG's cost was its size — around 138 million parameters, most of them in the final fully connected layers. That is the problem the next two architectures attacked.

---

## Inception and ResNet: Depth That Trains

**Inception** [@Szegedy2015] asked why a layer should commit to one filter size. Its module runs $1\times1$, $3\times3$ and $5\times5$ convolutions *in parallel* and concatenates the results, letting the network choose the scale. $1\times1$ convolutions reduce channel count first, keeping the cost down.

**ResNet** [@He2016] solved a different problem. Past a certain depth, deeper networks got *worse* — not from overfitting, but because gradients degraded on their way back through many layers. The fix is a **residual** or skip connection:

$$\mathbf{y} = \mathcal{F}(\mathbf{x}) + \mathbf{x}$$

Each block learns the *difference* from its input rather than a whole new representation, and the $+\,\mathbf{x}$ gives the gradient a direct path backward. That single change made networks of 100+ layers trainable, and skip connections now appear in nearly every deep architecture — including the Transformers in Lecture 9.

---

## What Carries Over to Time Series

These architectures were built for images, on problems that look nothing like weekly demand forecasting. Three of their ideas transfer anyway.

| | |
|---|---|
| **Small stacked filters** | A width-3 filter twice sees six weeks of context with far fewer weights than one width-6 filter. |
| **Skip connections** | Same fix for the same problem — gradients that fade through depth. |
| **Learned features** | You stop hand-engineering lag windows and let filters find the local shapes that matter. |

**One idea does not transfer, and it is the one we flagged:** pooling for translation invariance. An image classifier should not care where the cat is. A forecaster usually should care where the spike was.

---

# 1D CNNs for Time Series

The same machinery, along a time axis — and what it costs.

---

## Why Use a 1D CNN for Forecasting?

A 1D CNN takes a **window** of the recent past — say 26 weeks — across several channels, and predicts the next value. Each channel is one series: units, SNAP days, events, price.

The appeal is that you stop choosing lags. Rather than deciding that lag 1, lag 4 and lag 52 matter, you hand the model the whole window and let the filters learn which local shapes predict what comes next.

::: {.keybox}
**Two genuine advantages.** It is far cheaper than a fully connected network on the same window — weight sharing again. And it handles *many parallel series* naturally, since the same filters apply to every store-category at once.
:::

**The catch is the receptive field.** Stacked width-5 and width-3 filters see about seven weeks of context. Annual seasonality is 52 weeks away, and no reasonable stack of small filters reaches that far. Either you add dilation, or you feed the annual lag in as its own channel.

---

## 1D CNN Forecasting Pipeline in PyTorch

::: {.examplebox}
::: {.examplebox-title}
Window in, one forecast out
:::
```python
import torch.nn as nn

model = nn.Sequential(
    nn.Conv1d(n_channels, 32, kernel_size=5), nn.ReLU(),
    nn.Conv1d(32, 64, kernel_size=3),         nn.ReLU(),
    nn.AdaptiveAvgPool1d(1),   # collapses the time axis
    nn.Flatten(),
    nn.Linear(64, 1))
```
:::

Input shape is `(batch, channels, time)` — note that channels come *before* time, which is the transpose of how the data naturally sits in a dataframe.

Everything from Lecture 7 still applies: Adam with weight decay, `zero_grad()` every batch, `shuffle=False`, and a split by time rather than at random.

[`AdaptiveAvgPool1d(1)` averages across the whole time axis. Watch what that does on the next slide.]{.neutral .smaller}

---

## Does It Actually Work?

Fitted on our weekly panel — 26-week windows, four channels, 30 store-category series, the same test block as Homework 4.

| Model | Test RMSE | Parameters |
|---|---:|---:|
| LASSO (Homework 4) | **744** | 46 |
| XGBoost (Homework 4) | 781 | — |
| Random forest (Homework 3) | 899 | — |
| 1D CNN, flatten head | 1,032 | 8,161 |
| 1D CNN, global max pool | 1,418 | 6,945 |
| 1D CNN, global average pool | 1,534 | 6,945 |
| Seasonal naive | 2,152 | 0 |

[**Every CNN variant loses to a 46-coefficient linear model**]{.key-result} — a seven-week receptive field cannot see the annual lag LASSO was handed directly.

And swapping average pooling for `Flatten`, which keeps *where* each pattern occurred, is worth about **500 RMSE** — three seeds each, against a spread of about 80.

---

# Key Takeaways

---

## CNN Architecture Summary

| Architecture | Year | The idea that survived |
|---|---|---|
| LeNet-5 | 1998 | Conv–pool–conv–pool–dense: the template |
| VGG | 2014 | Only $3\times3$ filters, stacked deep |
| Inception | 2014 | Parallel filter sizes; $1\times1$ to cut channels |
| ResNet | 2015 | Skip connections make real depth trainable |
| 1D CNN | — | The same machinery along a time axis |

**Reach for a 1D CNN** when local shape matters more than exact position, when you have many parallel series, or as a cheap feature extractor feeding something else.

**Do not reach for one** when the dominant signal is at a long lag, when you have well engineered tabular features already, or when a stakeholder needs to read the model. On the evidence above, that covers most of what we forecast in this course.

---

## Lecture 8: Key Takeaways

::: {.keybox}
1. **Convolution** is local connectivity plus weight sharing. A pattern is learned once and detected everywhere, at a fraction of the parameters.

2. **Pooling** buys translation invariance — which an image classifier wants and a forecaster usually does not. Measured cost here: about 500 RMSE.

3. The architecture history reduces to three transferable ideas: **small filters stacked deep**, **skip connections** for trainable depth, and **learned** rather than hand-engineered features.

4. A **1D CNN** takes a window across channels and predicts the next step. Its weakness is the **receptive field**: seven weeks of context cannot see an annual lag.

5. On our panel every CNN variant **lost to a 46-coefficient LASSO**. Architecture has to fit the problem; it is not a ladder you climb.
:::

[Next: RNNs and LSTMs — architectures built to carry state through time, which is what the receptive field could not do.]{.neutral}

---

## References

::: {#refs}
:::
